# Mecz demo — plan wdrożenia

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Przycisk w Ustawieniach tworzy fikcyjny mecz (kick-off za 2 min, koniec po 3 min) przechodzący statusy sam w czasie — do obserwacji blokady typów o kick-offie i naliczania punktów po zakończeniu.

**Architecture:** Jedna kolumna `is_demo` na tabeli `matches`. Trzy akcje serwerowe (admin client) tworzą / przesuwają w czasie / kasują mecze demo. Kliencki `AutoRefresh` co ~30 s woła akcję przesuwającą status i odświeża stronę. Widoki punktów SQL bez zmian — działają na realnym `status`.

**Tech Stack:** Next.js 16 App Router, React 19, Supabase (Postgres + service-role client), shadcn/ui, sonner.

**Spec:** `docs/superpowers/specs/2026-05-22-mecz-demo-design.md`

**Weryfikacja w każdym zadaniu:** `npm run build`, `npm run typecheck`, `npm run lint` muszą przejść. To głównie integracja z Supabase — nie dopisujemy testów jednostkowych; `npm run test` (scoring) sprawdzany w zadaniu końcowym.

**Uwaga:** akcje używają klienta admina. Wzorzec: zobacz `app/(auth)/actions.ts` — `import { createAdminClient } from "@/lib/supabase/admin"`, użycie `const admin = createAdminClient()` (synchronicznie, bez `await`). Zwykły klient: `const supabase = await createClient()` z `@/lib/supabase/server`.

---

## Mapa plików

**Nowe:**
- `supabase/migrations/0004_demo_matches.sql` — kolumna `is_demo`
- `app/(app)/demo-actions.ts` — `createDemoMatch`, `settleDemoMatches`, `deleteDemoMatches`
- `components/auto-refresh.tsx` — kliencki tick: `settleDemoMatches` + `router.refresh()`
- `components/demo-panel.tsx` — kliencki panel z przyciskami (treść karty w Ustawieniach)

**Zmieniane:**
- `app/(app)/layout.tsx` — montaż `<AutoRefresh />`
- `app/(app)/settings/page.tsx` — karta „Tryb demo"

Uwaga: spec wskazywał `app/(app)/settings/demo-actions.ts`; plan przenosi plik akcji do `app/(app)/demo-actions.ts` (sibling istniejącego `app/(app)/actions.ts`), bo używa go zarówno panel w Ustawieniach, jak i `AutoRefresh` montowany w layoutcie — `settings/` byłby mylącym miejscem dla importu z layoutu.

---

## Task 1: Migracja i akcje serwerowe demo

**Files:**
- Create: `supabase/migrations/0004_demo_matches.sql`
- Create: `app/(app)/demo-actions.ts`

- [ ] **Step 1: Utwórz migrację `supabase/migrations/0004_demo_matches.sql`**

```sql
-- Mecz demo — flaga is_demo na matches.
-- Demo: mecz testowy przechodzący statusy w czasie (test blokady + punktacji).

alter table public.matches
  add column is_demo boolean not null default false;
```

- [ ] **Step 2: Utwórz `app/(app)/demo-actions.ts`**

```ts
"use server"

import { revalidatePath } from "next/cache"
import { createClient } from "@/lib/supabase/server"
import { createAdminClient } from "@/lib/supabase/admin"

export type DemoResult = { ok: boolean; error?: string }

const DEMO_KICKOFF_DELAY_MS = 2 * 60 * 1000 // kick-off za 2 minuty
const DEMO_DURATION_MS = 3 * 60 * 1000 // mecz trwa 3 minuty

function revalidateViews() {
  revalidatePath("/")
  revalidatePath("/siatka")
  revalidatePath("/ranking")
}

// Tworzy jeden mecz demo: kick-off za 2 min, status SCHEDULED, dwie losowe drużyny.
export async function createDemoMatch(): Promise<DemoResult> {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { ok: false, error: "Niezalogowany" }

  const admin = createAdminClient()

  // Dwie różne, losowe drużyny (lub null gdy brak danych).
  let team1Id: number | null = null
  let team2Id: number | null = null
  const { data: teams } = await admin.from("teams").select("id")
  if (teams && teams.length >= 2) {
    const shuffled = [...teams].sort(() => Math.random() - 0.5)
    team1Id = shuffled[0].id as number
    team2Id = shuffled[1].id as number
  }

  const kickoffAt = new Date(Date.now() + DEMO_KICKOFF_DELAY_MS).toISOString()

  const { error } = await admin.from("matches").insert({
    is_demo: true,
    stage: "GROUP",
    status: "SCHEDULED",
    kickoff_at: kickoffAt,
    team1_id: team1Id,
    team2_id: team2Id,
  })
  if (error) return { ok: false, error: error.message }

  revalidateViews()
  return { ok: true }
}

// Przesuwa statusy meczów demo w czasie. Idempotentne, bez wymogu logowania
// (czysto czasowa operacja, brak danych wejściowych). Wołane cyklicznie.
export async function settleDemoMatches(): Promise<DemoResult> {
  const admin = createAdminClient()
  const { data: matches, error } = await admin
    .from("matches")
    .select("id, kickoff_at, status, result1, result2")
    .eq("is_demo", true)
  if (error) return { ok: false, error: error.message }
  if (!matches || matches.length === 0) return { ok: true }

  const now = Date.now()

  for (const m of matches) {
    if (m.status === "FINISHED") continue
    const kickoffMs = new Date(m.kickoff_at as string).getTime()
    if (now < kickoffMs) continue // jeszcze przed kick-offem

    // Wynik ustawiamy raz, przy pierwszym przejściu w grę.
    let result1 = m.result1 as number | null
    let result2 = m.result2 as number | null
    if (result1 == null || result2 == null) {
      result1 = Math.floor(Math.random() * 5)
      result2 = Math.floor(Math.random() * 5)
    }

    const finished = now >= kickoffMs + DEMO_DURATION_MS
    const { error: updErr } = await admin
      .from("matches")
      .update({
        status: finished ? "FINISHED" : "IN_PLAY",
        result1,
        result2,
        updated_at: new Date().toISOString(),
      })
      .eq("id", m.id)
    if (updErr) return { ok: false, error: updErr.message }
  }

  return { ok: true }
}

// Kasuje wszystkie mecze demo (typy znikają kaskadą przez FK).
export async function deleteDemoMatches(): Promise<DemoResult> {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return { ok: false, error: "Niezalogowany" }

  const admin = createAdminClient()
  const { error } = await admin.from("matches").delete().eq("is_demo", true)
  if (error) return { ok: false, error: error.message }

  revalidateViews()
  return { ok: true }
}
```

- [ ] **Step 3: Sprawdź wzorzec klienta admina**

Przeczytaj `lib/supabase/admin.ts` i `app/(auth)/actions.ts`. Potwierdź, że `createAdminClient` jest eksportowany z `@/lib/supabase/admin` i wołany synchronicznie (`const admin = createAdminClient()`). Jeśli sygnatura jest inna (np. asynchroniczna) — dostosuj `demo-actions.ts` i zgłoś to.

- [ ] **Step 4: Weryfikacja**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: wszystkie przechodzą, zero ostrzeżeń. (Migracja `.sql` nie jest wykonywana przez build — sprawdzamy tylko kompilację TypeScriptu.)

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/0004_demo_matches.sql "app/(app)/demo-actions.ts"
git commit -m "feat: migracja is_demo + akcje serwerowe mecza demo"
```

---

## Task 2: Komponent AutoRefresh i montaż w layoutcie

**Files:**
- Create: `components/auto-refresh.tsx`
- Modify: `app/(app)/layout.tsx`

- [ ] **Step 1: Utwórz `components/auto-refresh.tsx`**

```tsx
"use client"

import { useEffect } from "react"
import { useRouter } from "next/navigation"
import { settleDemoMatches } from "@/app/(app)/demo-actions"

const REFRESH_INTERVAL_MS = 30_000

// Niewidoczny komponent: co ~30 s przesuwa statusy meczów demo w czasie
// i odświeża stronę, żeby mecz „działł się sam". Przydaje się też jako
// odświeżanie wyników na żywo podczas Mundialu.
export function AutoRefresh() {
  const router = useRouter()

  useEffect(() => {
    let cancelled = false

    const tick = async () => {
      await settleDemoMatches()
      if (!cancelled) router.refresh()
    }

    void tick() // przy zamontowaniu
    const id = setInterval(tick, REFRESH_INTERVAL_MS)

    return () => {
      cancelled = true
      clearInterval(id)
    }
  }, [router])

  return null
}
```

- [ ] **Step 2: Zamontuj `<AutoRefresh />` w `app/(app)/layout.tsx`**

Przeczytaj bieżący `app/(app)/layout.tsx`. Dodaj import:

```tsx
import { AutoRefresh } from "@/components/auto-refresh"
```

Wyrenderuj `<AutoRefresh />` obok istniejących `<BottomNav />` / `<Toaster />` (kolejność bez znaczenia — komponent zwraca `null`). Przykład docelowego fragmentu zwracanego JSX (dopasuj do faktycznej struktury pliku):

```tsx
      <main className="container mx-auto px-4 py-6 pb-24 lg:pb-6">{children}</main>

      <BottomNav />
      <Toaster />
      <AutoRefresh />
    </div>
```

Nie zmieniaj logiki pobierania użytkownika/profilu ani reszty layoutu.

- [ ] **Step 3: Weryfikacja**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: wszystkie przechodzą, zero ostrzeżeń.

- [ ] **Step 4: Commit**

```bash
git add components/auto-refresh.tsx "app/(app)/layout.tsx"
git commit -m "feat: AutoRefresh — cykliczne przesuwanie statusu meczow demo"
```

---

## Task 3: Panel demo w Ustawieniach

**Files:**
- Create: `components/demo-panel.tsx`
- Modify: `app/(app)/settings/page.tsx`

- [ ] **Step 1: Utwórz `components/demo-panel.tsx`**

```tsx
"use client"

import { useTransition } from "react"
import { toast } from "sonner"
import { Button } from "@/components/ui/button"
import { createDemoMatch, deleteDemoMatches } from "@/app/(app)/demo-actions"

export function DemoPanel() {
  const [pending, startTransition] = useTransition()

  const onCreate = () => {
    startTransition(async () => {
      const result = await createDemoMatch()
      if (result.ok) {
        toast.success("Mecz demo dodany — kick-off za 2 minuty.")
      } else {
        toast.error(result.error ?? "Nie udało się dodać meczu demo")
      }
    })
  }

  const onDelete = () => {
    startTransition(async () => {
      const result = await deleteDemoMatches()
      if (result.ok) {
        toast.success("Mecze demo usunięte.")
      } else {
        toast.error(result.error ?? "Nie udało się usunąć meczów demo")
      }
    })
  }

  return (
    <div className="space-y-3">
      <p className="text-muted-foreground text-sm">
        Tworzy mecz testowy z kick-offem za 2 minuty, kończący się 3 minuty
        później. Wpisz na niego typ w zakładce Mecze i kliknij „Zatwierdź
        typy" — punkty naliczają się tylko dla zatwierdzonych typów. Po
        kick-offie wiersz typu się zablokuje, a po zakończeniu meczu
        zobaczysz naliczone punkty w Rankingu.
      </p>
      <div className="flex flex-wrap gap-2">
        <Button
          onClick={onCreate}
          disabled={pending}
          className="font-display font-semibold"
        >
          Dodaj mecz demo
        </Button>
        <Button
          onClick={onDelete}
          disabled={pending}
          variant="outline"
          className="font-display font-semibold"
        >
          Usuń mecze demo
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Dodaj kartę „Tryb demo" w `app/(app)/settings/page.tsx`**

Przeczytaj bieżący `app/(app)/settings/page.tsx`. Dodaj import:

```tsx
import { DemoPanel } from "@/components/demo-panel"
```

Po istniejącej karcie „Zmiana hasła" (jako kolejna, ostatnia karta w kontenerze) dodaj kartę, dokładnie w stylu pozostałych kart na tej stronie (te same komponenty `Card`/`CardHeader`/`CardTitle`/`CardContent` i te same klasy `CardTitle` co pozostałe karty — sprawdź w pliku):

```tsx
      <Card>
        <CardHeader>
          <CardTitle className="font-display font-bold tracking-tight">Tryb demo</CardTitle>
        </CardHeader>
        <CardContent>
          <DemoPanel />
        </CardContent>
      </Card>
```

Jeśli klasy `CardTitle` pozostałych kart w pliku różnią się od powyższych — użyj tych samych co reszta pliku, dla spójności. Nie zmieniaj logiki strony.

- [ ] **Step 3: Weryfikacja**

Run: `npm run build && npm run typecheck && npm run lint`
Expected: wszystkie przechodzą, zero ostrzeżeń.

- [ ] **Step 4: Commit**

```bash
git add components/demo-panel.tsx "app/(app)/settings/page.tsx"
git commit -m "feat: panel demo w Ustawieniach (dodaj/usun mecz demo)"
```

---

## Task 4: Weryfikacja końcowa

**Files:** brak zmian — wyłącznie weryfikacja.

- [ ] **Step 1: Pełna weryfikacja**

Run: `npm run build && npm run typecheck && npm run lint && npm run test`
Expected: wszystkie cztery przechodzą; test scoringu (`lib/scoring.test.ts`) zielony.

- [ ] **Step 2: Sprawdzenie referencji**

Run: `grep -rn "demo-actions\|AutoRefresh\|DemoPanel\|settleDemoMatches" app components`
Expected: importy `demo-actions` w `auto-refresh.tsx` i `demo-panel.tsx`; `AutoRefresh` w `layout.tsx`; `DemoPanel` w `settings/page.tsx`. Brak osieroconych referencji.

- [ ] **Step 3: Notatka o migracji**

Migracja `supabase/migrations/0004_demo_matches.sql` musi zostać wgrana do Supabase ręcznie (Dashboard → SQL Editor lub Supabase CLI), tak jak poprzednie migracje. Bez tego akcje demo zwrócą błąd o nieznanej kolumnie `is_demo`. To krok wdrożeniowy poza kodem — odnotuj go w raporcie końcowym.

---

## Self-review — pokrycie spec

- Kolumna `is_demo` → Task 1 (migracja) ✓
- `createDemoMatch` (kick-off +2 min, SCHEDULED, losowe drużyny) → Task 1 ✓
- `settleDemoMatches` (IN_PLAY o kick-offie + losowy wynik; FINISHED po +3 min; obie reguły w jednym wywołaniu w kolejności) → Task 1 ✓
- `deleteDemoMatches` → Task 1 ✓
- Akcje przez klienta admina; `create`/`delete` wymagają logowania, `settle` nie → Task 1 ✓
- `AutoRefresh` (mount + co 30 s: `settleDemoMatches` + `router.refresh()`) → Task 2 ✓
- Montaż w `(app)/layout.tsx` → Task 2 ✓
- Panel demo widoczny dla wszystkich zalogowanych, opis + dwa przyciski + toast → Task 3 ✓
- Karta „Tryb demo" w `settings/page.tsx` → Task 3 ✓
- Widoki `prediction_points`/`leaderboard` nietknięte — żadne zadanie ich nie zmienia ✓
- Blokada/odsłanianie typów nietknięte — żadne zadanie nie zmienia RLS ani `prediction-cell.tsx` ✓
- Kryteria ukończenia (build/typecheck/lint/test + nota o migracji) → Task 4 ✓
